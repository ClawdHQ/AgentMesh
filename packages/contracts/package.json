{
  "name": "@agentmesh/contracts",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "hardhat compile",
    "test": "hardhat test",
    "deploy": "hardhat ignition deploy ignition/AgentMeshModule.ts --network baseSepolia",
    "deploy:local": "hardhat ignition deploy ignition/AgentMeshModule.ts --network localhost",
    "lint": "solhint 'contracts/**/*.sol'",
    "clean": "hardhat clean"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@nomicfoundation/hardhat-ignition": "^0.15.0",
    "@nomicfoundation/hardhat-ignition-ethers": "^0.15.0",
    "hardhat": "^2.22.0",
    "@openzeppelin/contracts": "^5.0.0",
    "typescript": "^5.4.5",
    "ts-node": "^10.9.2",
    "ethers": "^6.0.0",
    "viem": "^2.0.0",
    "chai": "^4.3.10",
    "@types/node": "^20.0.0"
  }
}
